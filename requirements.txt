# AIMNet2 hands-on tutorial. Python 3.11 or later, on Linux, Windows,
# or Apple Silicon macOS. See the install guide for per-platform notes.
aimnet[ase]>=0.2.0      # pulls torch>=2.8, ase>=3.27, warp-lang, numpy
warp-lang<1.18          # 1.18 wheels need NVIDIA driver 580 or newer
rdkit>=2024.3           # structure generation from SMILES
py3Dmol>=2.0            # interactive 3D views inside the notebooks
sella>=2.4              # saddle-point optimiser, notebook 5 only
matplotlib>=3.8         # plots
jupyterlab>=4.0         # to open the notebooks

# build tooling only: not needed to run the notebooks
jupytext>=1.16          # build.py regenerates notebooks/ from source/
